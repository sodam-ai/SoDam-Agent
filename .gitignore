node_modules/

# 비밀정보(.env) — 현재 프로젝트는 미사용이지만 향후 실수 방지용 사전 등록
.env
.env.*

# AgentRoster (백업·비밀 포함 가능 — Git에 올리지 않음)
.agentroster/

# 내부 전략 문서·세션 상태 (공개 저장소에 올리지 않음 — 필요 시 사용자 결정으로 포함)
.PRD/
.omc/
*.bak
CHECKPOINT.md
CHECKPOINT_ARCHIVE.md

# 플러그인이 만든 세션 산출물 (제품 아님 — 공개 제외)
.complexity-log.md
.todos-report.md/
.pair-programming-session.md
.plugin-config/
.active-agents
.active-agents.lock
.active-agents.tmp

# docs/ 산출물(PDF·HTML)은 빌드 결과물 — 저장소에 올리지 않음(2026-07-06 최종 결정)
docs/*.pdf
docs/*.html
