node_modules/

# AgentRoster (백업·비밀 포함 가능 — Git에 올리지 않음)
.agentroster/

# 내부 전략 문서·세션 상태 (공개 저장소에 올리지 않음 — 필요 시 사용자 결정으로 포함)
.PRD/
.omc/
*.bak

# 플러그인이 만든 세션 산출물 (제품 아님 — 공개 제외)
.complexity-log.md
.todos-report.md/
.pair-programming-session.md
.plugin-config/

# PDF는 빌드 산출물 — 저장소에 올리지 않음(2026-07-06 결정, 기존 4개는 git 추적 해제 필요)
# HTML(docs/*.html)은 2026-07-06부터 PDF 대체용 공개 문서로 추적함(더 이상 제외 안 함)
docs/*.pdf
