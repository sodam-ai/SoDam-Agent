# AgentRoster — 왕초보 가이드 (한국어)

[English Guide](./GUIDE.en.md) · [README(요약 설명서)](./README.md)

> 이 문서는 **AI·컴퓨터·프로그램 설치를 처음 해 보는 분**도 그대로 따라만 하면 되도록 만들었습니다.
> 어려운 말은 최대한 뺐고, 나오는 단어는 **그 자리에서 한 줄로** 풀이합니다.

---

## 0. 먼저 — 이게 뭐예요? (1분)

- **Claude Code**: AI에게 일을 시키는 프로그램이에요. 보통 AI **한 명**과 대화합니다.
- **AgentRoster**: 그 Claude Code에 **AI 직원 여러 명(팀)** 을 한 번에 붙여 주는 도구예요.
  예) *기획자 · 화면 개발자 · 서버 개발자 · 검토자* 를 한꺼번에 채용해 주는 느낌.
- 우리가 할 일: **명령 두 줄**을 Claude Code에 입력하는 것. 그게 전부예요.

> 💡 **용어 한 줄 풀이**
> - **플러그인**: 프로그램에 *끼워 쓰는 부품*. (AgentRoster가 바로 이 부품이에요.)
> - **마켓플레이스**: 부품을 *모아 둔 가게*. 여기에 등록하면 부품을 꺼내 쓸 수 있어요.
> - **에이전트(서브에이전트)**: AI 직원 한 명.
> - **입력창**: Claude Code 화면 아래쪽, 글자를 쳐 넣는 칸.

---

## 1. 준비 (딱 두 가지)

1. **Claude Code가 깔려 있어야 해요.** (없으면 먼저 설치: 검색창에 "Claude Code" → 공식 사이트 claude.com/claude-code)
2. **인터넷 연결.**

> (선택) `web-app-team`·`research-team`의 *자료 검색 도구*까지 쓰려면 **Node.js**가 필요해요. 없어도 AI 직원은 쓸 수 있고, 그 도구만 빠집니다. 필요하면 nodejs.org 에서 "LTS" 버튼을 눌러 설치하세요.

---

## 2. 설치하기 (그대로 따라 하기)

> 아래 글자는 **검은 터미널이 아니라, Claude Code 화면 아래 "입력창"** 에 칩니다.

### 1단계 · Claude Code 켜기
- 평소에 Claude Code를 켜던 방법으로 켜세요. (입력창이 보이면 준비 완료.)

### 2단계 · 가게(마켓플레이스) 등록 — 맨 처음 딱 한 번
👉 입력창에 아래를 **그대로** 치고 Enter:
```
/plugin marketplace add sodam-ai/AgentRoster
```
🖥️ "마켓플레이스가 추가되었다"는 안내가 나오면 성공.
> ⚠️ 혹시 *실패*하거나 *못 찾는다*고 나오면: 아직 공개 준비 중일 수 있어요. 그럴 땐 이 가이드 맨 아래 **"고급: 내려받아 써 보기"** 를 보세요.

### 3단계 · 팀 설치
👉 원하는 팀을 입력창에 치고 Enter (하나만 골라도 됩니다):
```
/plugin install web-app-team@agentroster
```
- 문서 팀을 원하면: `/plugin install docs-team@agentroster`
- 리서치 팀을 원하면: `/plugin install research-team@agentroster`

🖥️ "설치되었다"는 안내가 나오면 끝!

---

## 3. 잘 됐는지 확인하기

👉 입력창에 치고 Enter:
```
/agents
```
🖥️ AI 직원 목록이 떠요. 거기서 **`web-app-team:reviewer`** 같은 이름을 찾으세요.
- 이름 앞에 **`팀이름:`** 이 붙어 있어요 → *내가 깐 직원이 확실히 구분*됩니다. (이게 핵심!)
- 보이면 **성공**입니다. (다 봤으면 **Esc** 키로 빠져나오세요.)

---

## 4. AI 직원에게 일 시키기 (예시)

가장 쉬운 방법은 **말로 시키는 것**이에요. 입력창에 이렇게:
```
web-app-team:reviewer 한테 이 코드 좀 검토해 달라고 해줘
```
또는 글을 다루는 팀이면:
```
docs-team:editor 한테 이 글을 더 매끄럽게 다듬어 달라고 해줘
```
🖥️ Claude가 해당 직원(서브에이전트)을 불러서 일을 맡기고, 결과를 가져다 줍니다.

> 💡 입력창에 `@` 를 치면 직원 목록이 떠서 **골라서** 부를 수도 있어요.

---

## 5. 더 이상 필요 없을 때 (제거)

👉 입력창에 치고 Enter:
```
/plugin
```
🖥️ 설치한 플러그인 목록이 나와요. 지우고 싶은 팀을 골라 **제거(uninstall)** 를 누르면 끝.
> 제거는 Claude Code가 직접 안전하게 처리해서, 잘못 지워도 다시 설치하면 됩니다.

---

## 6. 막힐 때 (자주 있는 경우)

| 이런 일이 생기면 | 왜? | 이렇게 하세요 |
|---|---|---|
| `/plugin` 을 쳐도 안 먹혀요 | Claude Code가 옛날 버전 | Claude Code를 **최신으로 업데이트** |
| `marketplace add` 가 안 돼요 | 아직 공개 전이거나 주소 오타 | 공개된 뒤 다시 / 아래 "고급" 방법 |
| 경로를 넣었더니 "Invalid ... format" 같은 에러 | 경로에 **따옴표(")가 들어감** | 따옴표를 **빼고** 경로만. 공백·한글 없는 폴더로 |
| 등록은 됐는데 `/agents`에 **아무도 없어요** | `marketplace add`만 하고 **설치(`install`)를 안 함** | `/plugin install <팀>@agentroster` 를 **한 번 더** 실행 |
| 설치했는데 `/agents`에 안 보여요 | 아직 반영 안 됨 | `/reload-plugins` 입력 → 그래도 없으면 **Claude Code 껐다 켜기** |
| 자료 검색 도구가 오류 나요 | Node.js 없음/인터넷 막힘 | nodejs.org에서 LTS 설치 / 다른 인터넷 |
| 회사 인터넷에서 설치 실패 | 방화벽·프록시 차단 | 집 인터넷 등 다른 망에서 시도 |
| 휴대폰에서 쓰고 싶어요 | — | 이 도구는 **컴퓨터의 Claude Code 전용**이에요(휴대폰 단독 X) |

---

## 7. 안전 · 돈 관련 (꼭 알기)

- **지워도 안전**해요. 설치/제거는 Claude Code가 직접 관리합니다.
- **비밀번호·키는 파일에 저장되지 않아요.** (도구 설정엔 키 "이름"만 들어갑니다.)
- 이 도구는 **보증 없이(있는 그대로)** 제공돼요. 사용 책임은 본인에게 있어요.
- 설치하는 외부 도구(예: 자료검색 context7)나 외부 서비스의 **요금·약관은 본인이 직접 확인**하세요. (대부분 무료 한도가 있지만, 정책은 바뀔 수 있어요.)

---

## 8. 라이선스 (짧게)

- **Apache License 2.0** — 상업적 사용·수정·복제·재배포가 허용돼요(저작권 고지 유지·변경 표시·NOTICE 보존 조건). 자세한 건 `LICENSE`·`NOTICE` 파일.
- "Claude Code/Anthropic/Context7" 등은 각 회사의 이름(상표)이고, **AgentRoster는 비공식·미제휴**예요.

---

## 9. (고급) 마켓플레이스가 아직일 때 — 내 폴더로 미리 써 보기

마켓플레이스가 아직 공개 전이어도, **내려받은 폴더를 직접 "가게"로 등록**하면 됩니다. (검은 터미널 없이 클로드 안에서.)
1. 이 프로젝트를 내려받아요(GitHub에서 ZIP 다운로드 → 압축 풀기).
2. Claude Code **입력창**에 (⚠️ **따옴표는 빼고**, 공백·한글 없는 경로로):
   ```
   /plugin marketplace add C:\내려받은폴더\AgentRoster
   ```
3. 이어서 팀 설치 — **이 줄을 빠뜨리면 직원이 안 보여요**:
   ```
   /plugin install web-app-team@agentroster
   ```
4. 그다음은 위 **3·4단계(확인·사용)** 와 똑같아요.
> 💡 검은 터미널이 익숙하면 `claude --plugin-dir "내려받은폴더\plugins\web-app-team"` 도 됩니다.

---

> 더 자세한 설명서: [README.md](./README.md) · 🇺🇸 [English Guide](./GUIDE.en.md)
